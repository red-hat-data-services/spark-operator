arches:
  - x86_64
  - aarch64
  - ppc64le
  - s390x

contentOrigin:
  - ./ubi.repo

packages:
  # Java 17 OpenJDK for Spark operator
  - java-17-openjdk-devel
  # Tini - minimal init system for containers
  # Note: tini is in EPEL, will be configured via tekton prefetch options
  - tini
