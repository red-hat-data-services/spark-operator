arches:
  - x86_64
  - aarch64
  - ppc64le
  - s390x

contentOrigin:
  repofiles:
    - ./ubi.repo
    - ./epel.repo

packages:
  # Java 17 OpenJDK for Spark operator
  - java-17-openjdk-devel
  # Tini - minimal init system for containers (from EPEL)
  - tini
